# Optional. If unset, the app falls back to a deterministic mock planner so
# the demo still works end-to-end without a key.
GEMINI_API_KEY=

# Optional override. Defaults to gemini-2.5-flash.
GEMINI_MODEL=gemini-2.5-flash
